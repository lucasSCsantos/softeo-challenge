import styled from 'styled-components';
import { CardContainer } from '../../styles';

export const IncomeCardContainer = styled(CardContainer)``;

export const IncomeCardLabel = styled.p`
  color: ${props => props.theme.colors.label};
`;
